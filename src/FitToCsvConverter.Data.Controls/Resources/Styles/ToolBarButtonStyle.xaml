<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:FitToCsvConverter.Controls"
    xmlns:theme="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero2">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/FitToCsvConverter.Controls;component/Resources/Styles/ViewRessources.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type local:ToolBarButton}">
        <Setter Property="Background"
                Value="{StaticResource ExtraFileItemButtonBackgroundBrush}" />
        <Setter Property="Foreground"
                Value="{StaticResource TextBrush}" />
        <Setter Property="BorderThickness"
                Value="0" />
        <Setter Property="Padding"
                Value="5" />
        <Setter Property="HorizontalContentAlignment"
                Value="Stretch" />
        <Setter Property="VerticalContentAlignment"
                Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ToolBarButton}">
                    <Border x:Name="Border"
                        Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="MouseOver">
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="Border"
                                                        Storyboard.TargetProperty="(Background).(SolidColorBrush.Color)"
                                                        To="{StaticResource ExtraFileItemButtonHighlightColor}"
                                                        Duration="{StaticResource MouseOverAnimationDuration}" />
                                        <ColorAnimation Storyboard.TargetName="PART_LabelBorder"
                                                        Storyboard.TargetProperty="(Background).(SolidColorBrush.Color)"
                                                        To="{StaticResource ExtraFileItemButtonHighlightColor}"
                                                        Duration="{StaticResource MouseOverAnimationDuration}" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed" />
                                <VisualState x:Name="Disabled" />
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                            
                        <Grid Margin="0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Viewbox x:Name="PART_ContentPresenter"
                                     Grid.Row="0"
                                     Grid.RowSpan="2"
                                     Margin="{TemplateBinding Padding}"
                                     StretchDirection="Both"
                                     HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                     VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                                <ContentPresenter />
                            </Viewbox>

                            <Border x:Name="PART_LabelPlaceholderBorder"
                                    Grid.Row="1"
                                    Panel.ZIndex="0"
                                    Opacity="0"
                                    Background="Transparent"
                                    CornerRadius="{StaticResource CornerRadius}"
                                    VerticalAlignment="Center"
                                    Padding="5,0">
                                <TextBlock x:Name="PART_LabelPlaceholder"
                                           Text="{TemplateBinding LabelText}"
                                           FontSize="{TemplateBinding LabelFontSize}"
                                           Foreground="Transparent" />
                            </Border>
                            <Border x:Name="PART_LabelBorder"
                                    Grid.Row="1"
                                    Panel.ZIndex="10"
                                    Opacity="0"
                                    Background="{StaticResource ExtraFileItemButtonHighlightBrush}"
                                    CornerRadius="{StaticResource CornerRadius}"
                                    VerticalAlignment="Center"
                                    Padding="5,0">
                                <TextBlock x:Name="PART_Label"
                                           Text="{TemplateBinding LabelText}"
                                           HorizontalAlignment="Center"
                                           FontSize="0.01" />
                            </Border>
                        </Grid>
                    </Border>
            </ControlTemplate>
        </Setter.Value>
    </Setter>
    </Style>
</ResourceDictionary>